@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage
@using ContentModels = Umbraco.Cms.Web.Common.PublishedModels;


@*
<div>
    <ul>
   @foreach (var item in selection)
    {
    var typedMediaPickerSingle = item.Value<IPublishedContent>("Media");

 

        <li>
            <img src="@typedMediaPickerSingle.Url()"/>
        </li>
    }
 </ul>
</div>

@{
	var selection = Umbraco.Content(Guid.Parse("0caf96b6-8fca-4825-b6a7-ba80bd6489ab"))
    .ChildrenOfType("product")
    .Where(x => x.IsVisible());
}
<ul>
	@foreach (var item in selection)
	{
	var typedMediaPickerSingle = item.Value<IPublishedContent>("prouctContentImage");
		if (typedMediaPickerSingle != null)
		{
			<li>
				--<a href="@item.Url()">@item.Name()</a>
				<img src="@typedMediaPickerSingle.Url()" />
			</li>
		}
		
	}
</ul>
*@

<section class="l-section b-hero-banner b-hero-banner--text-left b-hero-banner--large-padding-bottom" style="background-image:url(assets/images/homepage-hero-background.jpg)">
	<div class="container">
		<div class="row">
			<div class="col-12">
				<div class="b-hero-banner__content">
					<p class="c-heading c-heading heading__xs c-heading--primary">@Model.Value("heroTitle")</p>
					<h1 class="c-heading c-heading--lg c-heading--white">@Model.Value("heroDescription")</h1>
					<a href="" class="c-button c-button--primary c-button--text-large btn">
						@Model.Value("homeHeroCTA")
					</a>
				</div>
			</div>
		</div>
	</div>
</section>